stages:
  - build-and-publish

variables:
  GIT_LFS_SKIP_SMUDGE: "0"

build_and_push_image:
  stage: build-and-publish
  image: docker:24-cli
  script:
    - sh scripts/ci_build_and_push.sh
  rules:
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG
